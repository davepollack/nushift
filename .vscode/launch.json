{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/nushift.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceRoot}/nushift",
            "environment": [],
            "console": "integratedTerminal"
        }
    ]
}
